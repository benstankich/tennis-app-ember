import Ember from 'ember';

export default Ember.Controller.extend({

	// loadPlugin: function() {
	// // Use run loop if you need to setup the DOM first
	// Ember.run.scheduleOnce('afterRender', this, function() {
	// 	Ember.$.getScript('../assets/scripts/chat.js');
	// });
	// }.on('init')

});